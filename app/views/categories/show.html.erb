<p>
	<strong>Name:</strong>
	<%= @category.name %>
</p>

<p>
	<strong>Items:</strong>
	<% @category.item_ids.each do |item_id| %>
  	<%= Item.find(item_id).name %><% if item_id != @category.item_ids.last %>;<% end %>
  <% end %> 
</p>

<%= link_to 'Back', categories_path %>